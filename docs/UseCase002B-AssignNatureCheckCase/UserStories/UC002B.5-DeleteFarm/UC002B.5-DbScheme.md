## Relational Database Schema – UC002B.5: Delete Farm

### **Stored Procedures for UC002B.5**

#### **usp_Farm_Delete**

**Purpose:** Deletes a farm if it has no active Nature Check Cases.

**Parameters:**
- `@FarmId` - `uniqueidentifier` - Farm ID to delete

**Logic:**
- Checks for active cases
- Prevents deletion if active cases exist
- Deletes farm if no active cases

---

### **Business Rules**

1. Farms with active cases cannot be deleted
2. Deletion cascades to related data (addresses, cases)


