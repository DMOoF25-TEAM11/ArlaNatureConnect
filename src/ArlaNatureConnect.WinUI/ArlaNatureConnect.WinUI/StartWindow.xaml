<Window
 x:Class="ArlaNatureConnect.WinUI.StartWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="using:ArlaNatureConnect.WinUI"
 xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
 Title="Starting...">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="ms-appx:///Assets/startUpImage.jpg" Stretch="UniformToFill" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel 
            Grid.Row="1"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            Spacing="12"
            Background="#AA000000">
            <TextBlock Text="Starting ArlaNatureConnect" FontSize="20" HorizontalAlignment="Center" Foreground="White"/>
            <muxc:ProgressBar IsIndeterminate="True" Width="400"/>
        </StackPanel>
    </Grid>
</Window>