<abstracts:SideMenuBaseUC
  x:Class="ArlaNatureConnect.WinUI.Views.Controls.SideMenu.ConsultantPageSideMenuUC"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:local="using:ArlaNatureConnect.WinUI.Views.Controls.SideMenu"
  xmlns:sidemenu="using:ArlaNatureConnect.WinUI.ViewModels.Controls.SideMenu" 
  xmlns:abstracts="using:ArlaNatureConnect.WinUI.Views.Controls.Abstracts" 
  d:DataContext="{d:DesignInstance Type=sidemenu:ConsultantPageSideMenuUCViewModel}"
  mc:Ignorable="d">


  <Grid>
    <Grid.RowDefinitions>
      <!-- Logo/Title Section -->
      <RowDefinition Height="Auto" />
      <!-- User Dropdown -->
      <RowDefinition Height="Auto" />
      <!-- Navigation Menu -->
      <RowDefinition Height="*" />
      <!-- Bottom Section -->
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>


    <!-- Logo/Title Section -->
    <Border Grid.Row="0" 
            Padding="24,28,24,24"
            BorderBrush="{StaticResource ArlaPrimaryDarkBrush}"
            BorderThickness="0,0,0,2">
      <StackPanel Orientation="Vertical">
        <StackPanel Spacing="20">
          <local:TitleAndLogoSideMenuUC/>
        </StackPanel>
        <!-- User Dropdown -->
        <StackPanel Spacing="12">
          <TextBlock  Text="Konsulent:" 
                      Foreground="{StaticResource ArlaPrimaryDarkBrush}"
                      FontSize="15"
                      FontWeight="SemiBold"
                      Opacity="0.9"/>
          <ComboBox x:Name="UserComboBox"
                    ItemsSource="{Binding AvailablePersons, Mode=OneWay}"
                    SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}}"
                    Style="{StaticResource ArlaComboBox}"
                    PlaceholderText="Vælg bruger">
            <ComboBox.ItemTemplate>
              <DataTemplate>
                <TextBlock>
                    <Run Text="{Binding FirstName}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding LastName}"/>
                </TextBlock>
              </DataTemplate>
            </ComboBox.ItemTemplate>
          </ComboBox>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- Navigation Menu -->
    <StackPanel Grid.Row="1" 
                        Spacing="12"
                        Padding="20,24">
      <Button x:Name="DashboardsButton"
                        Command="{Binding NavigationCommand}"
                        CommandParameter="Dashboards"
                        Content="Dashboards"
                        Style="{StaticResource ArlaNavButton}"/>

      <Button x:Name="FarmsButton"
                        Command="{Binding NavigationCommand}"
                        CommandParameter="Farms"
                        Content="Natur Check"
                        Style="{StaticResource ArlaNavButton}"/>

      <Button x:Name="TasksButton"
                        Command="{Binding NavigationCommand}"
                        CommandParameter="Tasks"
                        Content="Mine opgaver"
                        Style="{StaticResource ArlaNavButton}"/>
    </StackPanel>

    <!-- Bottom Section -->
    <StackPanel Grid.Row="3" 
                Spacing="12"
                Padding="20,24">
      <Button x:Name="LogoutButton"
                Content="Log ud"
                Command="{Binding LogoutCommand}"
                Style="{StaticResource ArlaNavButton}"/>
    </StackPanel>

  </Grid>
</abstracts:SideMenuBaseUC>
