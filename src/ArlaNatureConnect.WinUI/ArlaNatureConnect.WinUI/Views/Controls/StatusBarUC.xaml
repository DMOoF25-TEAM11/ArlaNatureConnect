<UserControl
    x:Class="ArlaNatureConnect.WinUI.Views.Controls.StatusBarUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArlaNatureConnect.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:ArlaNatureConnect.WinUI.ViewModels.Controls" d:DataContext="{d:DesignInstance Type=controls:StatusBarUCViewModel}"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8">
            <TextBlock Text="Busy:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <!-- Bind to symbol (string) instead of bool so UI shows emoji/text -->
            <TextBlock Text="{Binding BusySymbol}" VerticalAlignment="Center" FontSize="16" Margin="0,0,12,0" MinWidth="20"/>

            <TextBlock Text="DB:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <!-- Bind to symbol (string) instead of bool so UI shows emoji/text -->
            <TextBlock Text="{Binding DbConnectionSymbol}" VerticalAlignment="Center" FontSize="16" MinWidth="20"/>
        </StackPanel>
    </Grid>
</UserControl>
