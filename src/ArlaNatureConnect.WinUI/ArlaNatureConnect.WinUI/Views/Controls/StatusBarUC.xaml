<UserControl
    x:Class="ArlaNatureConnect.WinUI.Views.Controls.StatusBarUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArlaNatureConnect.WinUI.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewModels="using:ArlaNatureConnect.WinUI.ViewModels.Controls" 
    d:DataContext="{d:DesignInstance Type=viewModels:StatusBarUCViewModel}"
    mc:Ignorable="d">

  <!-- DataContext is provided at runtime from DI in code-behind. Keep d:DataContext for design-time only. -->
  <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
    <!-- Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"-->
    <TextBlock Text="Bruger:" Width="200" MaxWidth="200"/>

    <TextBlock Text="Status:"/>
    <!-- Bind to symbol (string) instead of bool so UI shows emoji/text -->
    <TextBlock Text="{Binding BusySymbol, Mode=OneWay}" Width="40"/>

    <TextBlock Text="DB:" />
    <!-- Bind to symbol (string) instead of bool so UI shows emoji/text -->
    <TextBlock Text="{Binding DbConnectionSymbol, Mode=OneWay}" Width="30"/>
  </StackPanel>
</UserControl>
