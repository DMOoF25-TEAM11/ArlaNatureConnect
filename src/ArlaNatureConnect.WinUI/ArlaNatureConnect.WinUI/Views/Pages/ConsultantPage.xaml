<Page
    x:Class="ArlaNatureConnect.WinUI.View.Pages.ConsultantPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArlaNatureConnect.WinUI.View.Pages"
    xmlns:consultant="using:ArlaNatureConnect.WinUI.View.Pages.Consultant"
    xmlns:domain="using:ArlaNatureConnect.Domain.Entities"
    Background="{StaticResource AppBackgroundBrush}"
    Loaded="ConsultantPage_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <consultant:ConsultantSidebar x:Name="Sidebar"
                                      Grid.Column="0"
                                      DataContext="{Binding}"/>

        <!-- Main Content Area - ContentPresenter for switching views -->
        <ContentPresenter x:Name="ContentPresenter"
                          Grid.Column="1"
                          DataContext="{Binding}"/>
    </Grid>
</Page>
