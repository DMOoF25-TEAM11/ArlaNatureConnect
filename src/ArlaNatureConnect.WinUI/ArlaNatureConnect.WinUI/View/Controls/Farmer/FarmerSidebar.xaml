<UserControl
    x:Class="ArlaNatureConnect.WinUI.View.Pages.Farmer.FarmerSidebar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArlaNatureConnect.WinUI.View.Pages.Farmer"
    Background="{StaticResource ArlaSidebarLightBrush}">

    <Border Background="{StaticResource ArlaSidebarLightBrush}"
            BorderBrush="{StaticResource ArlaPrimaryDarkBrush}"
            BorderThickness="0,0,2,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Logo/Title Section -->
            <Border Grid.Row="0" 
                    Padding="24,28,24,24"
                    BorderBrush="{StaticResource ArlaPrimaryDarkBrush}"
                    BorderThickness="0,0,0,2">
                <StackPanel Spacing="20">
                    <TextBlock Text="Arla Nature Connect" 
                               Foreground="{StaticResource ArlaPrimaryDarkBrush}"
                               FontSize="22"
                               FontWeight="Bold"
                               TextWrapping="Wrap"/>
                    
                    <!-- User Dropdown -->
                    <StackPanel Spacing="12">
                        <TextBlock Text="Landmand:" 
                                   Foreground="{StaticResource ArlaPrimaryDarkBrush}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Opacity="0.9"/>
                        <ComboBox x:Name="UserComboBox"
                                  ItemsSource="{Binding AvailablePersons}"
                                  SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                                  IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}}"
                                  Style="{StaticResource ArlaComboBox}"
                                  PlaceholderText="Vælg bruger">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <Run Text="{Binding FirstName}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding LastName}"/>
                                    </TextBlock>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <!-- Loading Indicator -->
                    <ProgressBar IsIndeterminate="True" 
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                                 Height="4"
                                 Background="Transparent"
                                 Foreground="{StaticResource ArlaAccentBrush}"/>
                </StackPanel>
            </Border>

            <!-- Navigation Menu -->
            <StackPanel Grid.Row="1" 
                        Spacing="12"
                        Padding="20,24">
                <Button x:Name="DashboardsButton"
                        Command="{Binding NavigationCommand}"
                        CommandParameter="Dashboards"
                        Content="Dashboards"
                        Style="{StaticResource ArlaNavButton}"/>

                <Button x:Name="FarmsButton"
                        Command="{Binding NavigationCommand}"
                        CommandParameter="Farms"
                        Content="Natur Check"
                        Style="{StaticResource ArlaNavButton}"/>

                <Button x:Name="TasksButton"
                        Command="{Binding NavigationCommand}"
                        CommandParameter="Tasks"
                        Content="Mine opgaver"
                        Style="{StaticResource ArlaNavButton}"/>
            </StackPanel>

            <!-- Bottom Section -->
            <Border Grid.Row="3" 
                    Padding="24">
                <!-- Can add logout button or other actions here -->
            </Border>
        </Grid>
    </Border>
</UserControl>

